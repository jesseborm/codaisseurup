<%= form_for [@event, @event.registrations.new] do |f| %>
  <p><strong>Event Price:</strong> <span>€</span> <%= @event.price %></p>

  <hr>

  <div class="form-group">
    <label>Status:</label>
    <%= f.text_field :status, :size => 50, placeholder: "Whatever status means to you" %>
  </div>

  <div class="form-group">
    <label>Number of guests (including yourself):</label>
    <%= f.select :guest_count, [["1", 1], ["2", 2],["3", 3]] %>
  </div>

  <hr>

  <div class="actions">
    <%= f.submit "Register Now", class: "btn btn-success" %>
  </div>
<% end %>
